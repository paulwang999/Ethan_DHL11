\# DHT11 Micro:bit Extension



This extension allows Micro:bit to read temperature and humidity from DHT11 sensor.



\- Use `DHT11.readData(DigitalPin.P0)` to read sensor

\- Use `DHT11.temperature()` to get temperature

\- Use `DHT11.humidity()` to get humidity



